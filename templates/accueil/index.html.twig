{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}
    <h2>Liste des articles</h2>
    <ul >
        {% for article in articles %}
                <li><a href="{{ path('app_accueil') }}"></a><h3>{{ article.titre }}</h3></li>
                <a href="{{ path('app_article_slug',{"slug":article.slug}) }}">Détail</a>
                <p>Article du {{ article.createdat | date('d/m/Y') }}</p>
        {% endfor %}
    </ul>
{% endblock %}
